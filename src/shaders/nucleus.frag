#version 330 core


// ============================================================
// INPUT
// ============================================================

in vec3 worldNormal;
in vec3 worldPosition;


// ============================================================
// UNIFORMS
// ============================================================

uniform vec3 particleColor;


// ============================================================
// OUTPUT
// ============================================================

out vec4 FragColor;


// ============================================================
// MAIN
// ============================================================

void main()
{
    // --------------------------------------------------------
    // Normal
    // --------------------------------------------------------

    vec3 normal =
        normalize(worldNormal);


    // --------------------------------------------------------
    // Dirección de la luz
    // --------------------------------------------------------

    vec3 lightDirection =
        normalize(
            vec3(
                -0.5,
                1.0,
                0.3
            )
        );


    // --------------------------------------------------------
    // Iluminación difusa
    // --------------------------------------------------------

    float diffuse =
        max(
            dot(
                normal,
                lightDirection
            ),
            0.0
        );


    // --------------------------------------------------------
    // Iluminación ambiental
    // --------------------------------------------------------

    float ambient =
        0.25;


    // --------------------------------------------------------
    // Iluminación final
    // --------------------------------------------------------

    float lighting =
        ambient
        +
        diffuse * 0.75;


    // --------------------------------------------------------
    // Color
    // --------------------------------------------------------

    FragColor =
        vec4(
            particleColor * lighting,
            1.0
        );
}
